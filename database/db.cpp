#include "db.hpp"
#include <sqlite3.h>
#include <iostream>

std::vector<User> getAllUsersFromDB() {
    // (Write code to connect to DB and return all users)
}

void addUserToDB(const User& user) {
    // (Write code to insert user into DB)
}
